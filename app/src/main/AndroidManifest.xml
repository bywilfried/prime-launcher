<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to query installed apps (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Permission to request app uninstall -->
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />

    <!-- Permission for persistent notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permission to start on boot (for restoring notification) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Permission to expand notification shade -->
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />

    <!-- Legacy wallpaper read for Android <= 12 only. READ_MEDIA_IMAGES was
         removed: Google Play's photo/video permissions policy forbids it for a
         launcher (only occasional media access, handled via the system photo
         picker). On Android 13+ the wallpaper preview falls back to the built-in
         wallpaper drawable — see ScreenPreview's guarded getDrawable(). -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Permission to set the actual Android system wallpaper via WallpaperManager -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />


    <!-- Shortcut support -->
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />

    <!-- Direct dial 1x1: one-tap call (runtime, requested on add; denied -> dialer pre-filled) -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Recent Apps view (issue #40). PACKAGE_USAGE_STATS is a "special" permission;
         the user grants it manually via Settings → Apps → Special access → Usage access. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Launcher314"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.Launcher314">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.content.pm.action.CONFIRM_PIN_APPWIDGET" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ShortcutChoiceActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Launcher314.Launcher">
            <intent-filter>
                <action android:name="android.content.pm.action.CONFIRM_PIN_SHORTCUT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.LegacyShortcutChoiceActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Launcher314.Launcher" />

        <!-- App Drawer Accessibility Service -->
        <service
            android:name=".services.AppDrawerAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Accessibility Service Settings Activity (shown in Android accessibility settings) -->
        <activity
            android:name=".activities.AccessibilitySettingsActivity"
            android:exported="true"
            android:label="App Drawer"
            android:theme="@style/Theme.Launcher314" />

        <!-- Quick Settings Tile Service -->
        <service
            android:name=".services.AppDrawerTileService"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="App Drawer"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Notification Action Receiver -->
        <receiver
            android:name=".services.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.bearinmind.launcher314.ACTION_OPEN_DRAWER" />
            </intent-filter>
        </receiver>

        <!-- Install Shortcut Receiver (for "Add to Home Screen" from browsers) -->
        <receiver
            android:name=".services.InstallShortcutReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.launcher.action.INSTALL_SHORTCUT" />
            </intent-filter>
        </receiver>

        <!-- Boot Completed Receiver (to restore notification on boot) -->
        <receiver
            android:name=".services.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>